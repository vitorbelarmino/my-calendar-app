export interface CalendarEvent {
  id: string;
  title: string;
  description: string;
  themeColor: string;
  date: string;
  hour: string;
  userId: string;
  createdAt: string;
  updatedAt: string;
}
